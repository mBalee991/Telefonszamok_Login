<Window x:Class="Telefonszamok.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bejelentkezés" Height="200" Width="350" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Felhasználónév:"/>
        <TextBox x:Name="tbFelhasznalo" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Jelszó:"/>
        <PasswordBox x:Name="pbJelszo" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2"
                Content="Bejelentkezés"
                HorizontalAlignment="Center"
                Width="120"
                Margin="10"
                Click="btBelepes_Click"/>

    </Grid>
</Window>
